#include<iostream>
using namespace std;
string calculateHotelPrices(string month, int numberOfStays);
main()
{
    cout<<"Enter the month (May, June, July, August, September, October): ";
    cin>>m;
    cout<<"Enter the number of stays: ";
    cin>>n;
    calculateHotelPrices(m,n);
}
string calculateHotelPrices(string month, int numberOfStays);
{
    
}
